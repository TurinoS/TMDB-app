import styled from "styled-components";

const StyledCard = styled.div`
    width: 20vw;
`

export default function Card() {
    return(
        <StyledCard>
            <h2>Card</h2>
        </StyledCard>
    )
}